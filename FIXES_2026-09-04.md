# Saraya Inventory — fixes 2026-09-04

Implemented:
- End-shift expenses and expense notes are persisted and shown in admin details.
- Automatic manque amount/note from expected final recette minus real cash counted.
- Real cash counted is shown in admin Financial Breakdown.
- Active shift admin monitoring: start time, worker, opening front stock, additions, and ENREGISTREMENT/CAISSE status.
- Persistent end-shift recording stage survives refresh/reconnection.
- Rami paper stock is visible in Stock Management and works for workers with the active shift.
- Rami paper opening is read-only during end-shift saisie.
- Manual stock correction keeps the interface populated without requiring a page refresh.
- New products default to warehouse storage and expose Front/Warehouse location settings.
- Warehouse-only products can be counted at end shift instead of being forced to front stock.
- Removed Eau 0.5 utilisée pour Chicha from ticket input.
- Removed Staff Cannette from worker UI/financial presentation.
- Chicha price is configurable by admin in Stock Management; default is 7 DT.
- Chicha/Eau 0.5 classification uses configured Chicha price and the actual Eau 0.5 product price.
- Phase 2 correction no longer duplicates Eau 0.5 + Kamia; classification handles that separately.
- Worker finalization shows Chicha and coffee/beans performance.

Database migration:
- database/migration_shift_v3.sql

Validation:
- Backend JS syntax checked successfully.
- JSX parser validation checked successfully for ShiftManagement.jsx and StockManagement.jsx.
- Full Vite build was not run because the supplied Windows node_modules contains a Windows esbuild binary; on Windows, run `npm install` in client before `npm run dev`/`npm run build`.
